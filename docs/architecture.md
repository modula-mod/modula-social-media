# Architecture

The Social Media module is the first canonical Modula module. It ships marketplace metadata in `manifest.json`, runtime metadata in `module.json`, a host-rendered bridge via `runtime_renderer: social_host`, and a sandbox bundle entry for future isolated execution.
